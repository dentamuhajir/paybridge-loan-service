spring:
  application:
    name: paybridge-loan-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/paybridge_loan}
    username: ${SPRING_DATASOURCE_USERNAME:dentamuhajir}
    password: ${SPRING_DATASOURCE_PASSWORD:5MbVqyTEZ42wFS4as9iwZ7}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
